// Stub declarations for untyped packages, so `.ts` files can import them
// without "could not find a declaration file" errors. Each becomes `any`.
// Replace with real types (or @types/*) as a package gains them.
declare module '@sipec/vue3-tags-input'
